# Hetzner Cloud API token (required)
# Get one at: https://console.hetzner.cloud → Project → Security → API Tokens
HCLOUD_TOKEN=

# Anthropic API key for Claude Code (recommended)
# Get one at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# OpenAI API key for Codex (recommended)
# Get one at: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# ── Optional overrides (auto-detected if not set) ──────────────

# Path to SSH public key (default: ~/.ssh/id_ed25519.pub or ~/.ssh/id_rsa.pub)
# SSH_PUBLIC_KEY_PATH=~/.ssh/id_ed25519.pub

# Git identity (default: read from local git config)
# GIT_USER_NAME=
# GIT_USER_EMAIL=
